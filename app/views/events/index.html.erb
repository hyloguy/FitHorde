
<p id="notice"><%= notice %></p>

<div id="map-canvas"></div>
<div class="container-fluid col-md-12" id="main">
	<div class="row">
		<div class="col-sm-9"><!-- Map goes here! --></div>
		<div class="col-sm-3">
			<div class="list-group">
				<% @events.each do |event| %>
					<a href="#" id="<%= event.id %>" type="button"
						class="list-group-item"
						data-toggle="popover"
						data-html="true"
						data-container="body"
						data-placement="left"
						data-title="Event"
						data-eventid="<%= event.id %>">
						<%= event.title %>
					</a>
				<% end %>
				<%= link_to "Create new event", new_event_path,
						:class => 'list-group-item' %>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
				<a href="#" class="list-group-item">Some Event</a>
			</div>
		</div>
	</div>
</div>

<!-- Remove this below -->
<div class="container-fluid">
	<%= link_to "Create new event", new_event_path,
			:class => 'btn btn-primary btn-sm' %>
</div>
<!-- Remove this above -->

<% @events.each do |event| %>
	<div id="popover-content-event-<%= event.id %>" class="hide">
		<p>title: <%= event.title %></p>
		<p>description: <%= event.description %></p>
		<p>street: <%= event.street %></p>
		<p>city: <%= event.city %></p>
		<p>state: <%= event.state %></p>
		<p>starting: <%= event.starting %></p>
		<p>duration: <%= event.duration %></p>
		<p>capacity: <%= event.capacity %></p>
		<p>organizer: <%= event.user.firstname %></p>
		<p><%= link_to "Edit", edit_event_path(event),
										:class => 'btn btn-default btn-xs' %>
		</p>
		<p>
			<%= link_to "Delete", event, method: :delete,
		                data: { confirm: 'Are you sure?' },
		                :class => 'btn btn-danger btn-xs'  %>
		</p>
	</div>
<% end %>
